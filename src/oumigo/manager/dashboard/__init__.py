"""Performance & diagnostics dashboard (later).

Reads control-plane state (node registry, lifecycle) and data-plane request
metrics — a consumer of what the other two planes already collect, not a new
source of truth.
"""
